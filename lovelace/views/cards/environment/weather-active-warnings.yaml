type: conditional
conditions:
  - entity: sensor.active_weather_warnings
    state_not: "unknown"
card:
  type: vertical-stack
  cards:
    - type: markdown
      style: !include styles/bg-red.yaml
      content: >
        {{state_attr('sensor.active_weather_warnings', 'warning1')}}

    - type: markdown
      style: !include styles/bg-red.yaml
      content: >
        {{state_attr('sensor.active_weather_warnings', 'warning2')}}
