type: custom:vertical-stack-in-card
title: !secret glances_3_name
cards:
  - type: horizontal-stack
    cards:
      - type: 'custom:mini-graph-card'
        style: !include styles/mini-graph.yaml
        name: Container count
        animate: true

        show:
          average: true
          extrema: true

        entities:
          - entity: sensor.pinkie_containers_active

      - type: 'custom:mini-graph-card'
        style: !include styles/mini-graph.yaml
        name: Thread count
        animate: true

        show:
          average: true
          extrema: true

        entities:
          - entity: sensor.pinkie_thread

      - type: 'custom:mini-graph-card'
        style: !include styles/mini-graph.yaml
        name: CPU used
        animate: true

        show:
          average: true
          extrema: true

        entities:
          - entity: sensor.pinkie_cpu_used

      - type: 'custom:mini-graph-card'
        style: !include styles/mini-graph.yaml
        name: RAM used
        animate: true

        show:
          average: true
          extrema: true

        entities:
          - entity: sensor.pinkie_ram_used_percent
