automation: !include_dir_merge_list automations
zone: !include zone.yaml

# Enables configuration UI
config:

stream:
  # mobile_app:

system_health:

# Enable generating a readme file
readme:

recorder:
  db_url: !secret postgres_url
  purge_keep_days: 7

http:
  # Need to define URL as HA is running behind a reverse proxy
  base_url: !secret http_base_url
  trusted_proxies: !secret trusted_proxies
  use_x_forwarded_for: !secret use_x_forwarded_for

# Checks for available updates
# Note: This component will send some information about your system to
# the developers to assist with development of Home Assistant.
# For more information, please see:
# https://home-assistant.io/blog/2016/10/25/explaining-the-updater/
updater:
  # Optional, allows Home Assistant developers to focus on popular components.
  include_used_components: true

# Enables support for tracking state changes over time
history:

# View all events in a logbook
logbook:
  exclude:
    entities:
      - automation.loop_reply_to_loop_tick_event
      - script.loop_tick
      - automation.update_wake_up_sensor_every_minute

# Enables a map showing the location of tracked devices
map:

# Track the sun
sun:

# Enables running custom scripts
python_script:

# Register the git hook script
shell_command:
  git_pull: bash /config/shell_scripts/git-pull.sh
