description: Lock a device
fields:
  target:
    description: An array of target devices to lock
    example: ***REMOVED*** A6003
sequence:
  - service: notify.automate
    data_template:
      target: >
        {{target}}
      message: >
        {"type": "lock"}
