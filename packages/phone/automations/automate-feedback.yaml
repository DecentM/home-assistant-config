- alias: Automate - Receive feedback and route to service
  trigger:
    platform: webhook
    webhook_id: !secret automate_webhook_id
  condition:
    - condition: template
      value_template: >
        {{trigger.json.feedbackKey != None}}
  action:
    service_template: >
      script.{{trigger.json.feedbackKey}}
    data_template:
      data: "{{trigger.json}}"
